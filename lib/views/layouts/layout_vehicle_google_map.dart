import 'package:flutter/material.dart';

class LayoutVehicleGoogleMap extends StatelessWidget {
  const LayoutVehicleGoogleMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color:Colors.red,
    );
  }
}
