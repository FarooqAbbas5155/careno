import 'package:flutter/material.dart';

class ScreenHostHomePage extends StatelessWidget {
  const ScreenHostHomePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
