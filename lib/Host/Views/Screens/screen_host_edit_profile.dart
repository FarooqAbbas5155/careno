import 'package:flutter/material.dart';

class ScreenHostEditProfile extends StatelessWidget {
  const ScreenHostEditProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
