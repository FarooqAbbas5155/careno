import 'package:flutter/material.dart';

class ScreenHostBookingDetail extends StatelessWidget {
  const ScreenHostBookingDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
