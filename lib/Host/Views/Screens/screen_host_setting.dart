import 'package:flutter/material.dart';

class ScreenHostSetting extends StatelessWidget {
  const ScreenHostSetting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
